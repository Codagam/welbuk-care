/**
 * Re-export appointment types for the clinical today-queue (compat).
 * Full list types live in `@/features/appointments/types`.
 */
export type {
  Appointment,
  AppointmentDoctor,
  AppointmentPatient,
  AppointmentSearchParams,
  AppointmentSearchResult,
} from "@/features/appointments/types";

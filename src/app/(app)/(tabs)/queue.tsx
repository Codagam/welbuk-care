import { AppointmentListScreen } from "@/features/appointments/components/AppointmentListScreen";

/** Queue and appointment list are the same screen. */
export default function QueueScreen() {
  return <AppointmentListScreen />;
}
